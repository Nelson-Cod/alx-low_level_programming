#include "main.h"
/**
 * print_alphabet - prints alphabets in lowercase
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
char alpha;
for (alpha = 97; alpha <= 122, alpa++)
	_putchar(alpha);
_putchar('\n');
}
